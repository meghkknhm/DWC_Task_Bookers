<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<h1>Editing Book</h1>
<%= form_with model:@book, url:"/todolists/#{@book.id}", local:true do |f| %><% if @book.errors.any? %>
    <%= @book.errors.count %>errors prohibited this book from being saved<br>
    <% @book.errors.full_messages.each do |message| %>
      <%= message %><br>
    <% end %>
  <% end %>

  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Update Book' %>
  <%= link_to "Show",todolist_path(@book.id) %>
  <%= link_to "Back", index_todolist_path %>
<% end %>